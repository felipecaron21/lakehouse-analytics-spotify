name: spotify_lakehouse
version: '1.0.0'
config-version: 2

profile: spotify_lakehouse

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  spotify_lakehouse:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
